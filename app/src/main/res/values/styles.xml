<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Default filled button: comfortable padding, readable non-tiny text. -->
    <style name="AppTheme.Button" parent="Widget.Material3.Button">
        <item name="android:minWidth">0dp</item>
        <item name="android:paddingStart">24dp</item>
        <item name="android:paddingEnd">24dp</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Drawer: brand-coloured dividers between menu groups, and a slightly tighter row than
         Material3's default so the longer entries (Steam / GOG / import-export) fit on one line.
         Size and padding are declared EXPLICITLY rather than left to inherit: they are tuned
         values now, and an inherited default would silently drift on the next Material update.
         Baseline they were tuned from: Widget.Material3.NavigationView = 14sp text, 28dp
         horizontal padding. -->
    <style name="AppTheme.NavigationView" parent="Widget.Material3.NavigationView">
        <item name="dividerColor">@color/rd_primary</item>
        <item name="itemTextAppearance">@style/AppTheme.NavigationView.Item</item>
        <item name="itemHorizontalPadding">23dp</item>
    </style>

    <style name="AppTheme.NavigationView.Item" parent="TextAppearance.Material3.LabelLarge">
        <item name="android:textSize">13sp</item>
    </style>

    <!-- Borderless, tinted 48dp icon button (e.g. the instance-card gear). -->
    <style name="AppTheme.ImageButton">
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
        <item name="android:tint">?attr/colorOnSurfaceVariant</item>
    </style>

    <!-- Tiny caption under each drawer-footer link icon, so the glyphs are self-explanatory. -->
    <style name="AppTheme.NavBottomLabel" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:textSize">9sp</item>
        <item name="android:textColor">?attr/colorOnSurfaceVariant</item>
        <item name="android:singleLine">true</item>
    </style>

    <!-- Section header (e.g. on the Settings / Download screens). -->
    <style name="AppTheme.SectionTitle" parent="TextAppearance.Material3.TitleMedium">
        <item name="android:textColor">?attr/colorPrimary</item>
    </style>

    <!-- Card header (Settings cards): small + brand-accent, Zomdroid-scale. Compact and coloured
         instead of a big white title, so the cards read as grouped sections at a glance. -->
    <style name="AppTheme.CardHeader" parent="TextAppearance.Material3.LabelSmall">
        <item name="android:textColor">?attr/colorPrimary</item>
        <item name="android:textSize">13sp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- App dialogs (instance picker, confirmations, etc.): M3 rounded corners (default) + brand-coloured
         title. Wired into AppTheme via materialAlertDialogTheme, so every MaterialAlertDialogBuilder uses it. -->
    <style name="ThemeOverlay.ValDroid.Dialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="materialAlertDialogTitleTextStyle">@style/ValDroid.Dialog.Title</item>
    </style>

    <style name="ValDroid.Dialog.Title" parent="MaterialAlertDialog.Material3.Title.Text">
        <item name="android:textColor">?attr/colorPrimary</item>
    </style>

    <!-- Controls editor toolbar: separate outlined buttons floating over the layout (white contour on
         a translucent dark fill, readable over any background), instead of text links on a black bar. -->
    <style name="AppTheme.EditorToolbarButton" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:layout_marginStart">3dp</item>
        <item name="android:layout_marginEnd">3dp</item>
        <item name="android:minHeight">40dp</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:paddingStart">14dp</item>
        <item name="android:paddingEnd">14dp</item>
        <item name="android:textSize">13sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">#FFFFFFFF</item>
        <item name="backgroundTint">#99000000</item>
        <item name="strokeColor">#E6FFFFFF</item>
        <item name="strokeWidth">1.5dp</item>
        <item name="cornerRadius">12dp</item>
    </style>

    <!-- The toolbar's main action (Done): filled with the theme colour. -->
    <style name="AppTheme.EditorToolbarButton.Primary" parent="Widget.Material3.Button">
        <item name="android:layout_marginStart">3dp</item>
        <item name="android:layout_marginEnd">3dp</item>
        <item name="android:minHeight">40dp</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:textSize">13sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">12dp</item>
    </style>
</resources>
